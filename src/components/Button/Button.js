import React from "react";
import "./Button.css";

const Button = () => {
  return (
    <button type="button" className="btn btn-success">
      Send message
    </button>
  );
};

export default Button;
